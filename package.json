{
  "name": "game-guild",
  "version": "0.1.0",
  "description": "GameGuild Platform Monorepo",
  "author": "Matheus Martins <contact@matheusmartins.com>",
  "homepage": "https://gameguild.gg",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "dev:api": "dotnet run --project apps/api/Source/GameGuild.API/GameGuild.API.csproj --urls http://localhost:5295",
    "dev:web": "pnpm --filter @gameguild/web run dev",
    "build": "turbo run build",
    "build:web": "pnpm --filter @gameguild/web run build",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage",
    "test:e2e": "turbo run test:e2e",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "format": "pnpm -r run format",
    "generate": "turbo run generate",
    "sync:format": "syncpack format",
    "sync:list": "syncpack list",
    "sync:lint": "syncpack lint",
    "sync:fix": "syncpack fix-mismatches",
    "clean": "rimraf --glob ./**/node_modules ./**/dist ./**/coverage ./**/build ./**/.next ./**/.turbo pnpm-lock.yaml",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "turbo run build && changeset publish"
  },
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.0",
    "@changesets/cli": "^2.27.0",
    "@game-guild/eslint-config": "workspace:*",
    "@game-guild/prettier-config": "workspace:*",
    "@game-guild/typescript-config": "workspace:*",
    "eslint": "^9.18.0",
    "prettier": "^3.5.0",
    "rimraf": "^6.0.0",
    "syncpack": "^13.0.4",
    "turbo": "^2.8.20",
    "typescript": "^5.8.3"
  }
}