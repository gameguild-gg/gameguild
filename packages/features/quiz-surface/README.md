# @game-guild/quiz-surface

Controlled React authoring, collection, learner, and local-practice surfaces for `@game-guild/quiz`.

The package owns the quiz-specific block collection UX, but not the host's
persisted block schema, HTTP integration, or trusted grading policy.
