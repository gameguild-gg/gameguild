{
    "branches": [
        "main"
    ],
    "plugins": [
        "@semantic-release/commit-analyzer",
        [
            "@semantic-release/exec",
            {
                "generateNotesCmd": "LAST_GIT_TAG='${lastRelease.gitTag}' NEXT_GIT_TAG='${nextRelease.gitTag}' NEXT_VERSION='${nextRelease.version}' REPO_URL='${options.repositoryUrl}' RELEASE_DATE=\"$(date -u +%Y-%m-%d)\" node scripts/devops/sr-generate-notes.mjs",
                "prepareCmd": "node scripts/devops/sr-prepare.mjs ${nextRelease.version}"
            }
        ],
        [
            "@semantic-release/changelog",
            {
                "changelogFile": "CHANGELOG.md"
            }
        ],
        [
            "@semantic-release/git",
            {
                "assets": [
                    "CHANGELOG.md",
                    "package.json",
                    "apps/*/package.json",
                    "packages/*/package.json",
                    "packages/config/*/package.json",
                    "tools/*/package.json",
                    "tools/emception/packages/*/package.json",
                    "tools/emception/apps/*/package.json"
                ],
                "message": "chore(release): ${nextRelease.version} [skip ci]"
            }
        ],
        "@semantic-release/github"
    ]
}
