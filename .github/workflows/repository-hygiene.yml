name: Repository Hygiene

on:
  pull_request:
  push:
    branches:
      - develop
      - main

permissions:
  contents: read

jobs:
  validate:
    name: Validate tracked artifacts
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7 # v4
        with:
          fetch-depth: 0

      - name: Validate repository hygiene
        run: bash scripts/repository-hygiene.sh
