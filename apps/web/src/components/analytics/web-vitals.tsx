'use client';

import { useReportWebVitals } from 'next/web-vitals';

export const WebVitals = (): null => {
  useReportWebVitals(() => {
    // Web vitals reporting is currently disabled
    // To enable, uncomment the implementation below and add required dependencies
  });

  return null;
};
