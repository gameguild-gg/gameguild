import React from 'react';

/**
 * Course Forbidden (403)
 *
 * Displayed when the user lacks permission to access this course.
 * Triggered by calling `forbidden()` from 'next/navigation' in page/layout.
 *
 * Next.js will return a 403 status code automatically.
 *
 * @see https://nextjs.org/docs/app/api-reference/functions/forbidden
 */
export default function Forbidden(): React.JSX.Element {
  // TODO: Implement forbidden UI with:
  // - "Access Denied" message
  // - Explanation of why access is forbidden
  // - Link back to courses list
  // - Contact support/request access option

  return <></>;
}
