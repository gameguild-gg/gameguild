import React from 'react';

/**
 * L2: Courses List Loading State
 *
 * Streaming fallback while fetching courses list.
 * This enables progressive rendering with Suspense boundaries.
 */
export default function Loading(): React.JSX.Element {
  // TODO: Implement skeleton UI for:
  // - Course grid/list with card skeletons (6-12 items)
  // - Each card: thumbnail placeholder, title, status badge, KPI values
  return <></>;
}
