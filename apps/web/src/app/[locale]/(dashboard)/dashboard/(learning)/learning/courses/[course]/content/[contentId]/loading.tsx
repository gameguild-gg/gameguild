import React from 'react';

/**
 * Loading UI for Content Item Editor route.
 * Shown while getContentItem() resolves.
 */
export default function Loading(): React.JSX.Element {
  return <div>Loading content item...</div>;
}
