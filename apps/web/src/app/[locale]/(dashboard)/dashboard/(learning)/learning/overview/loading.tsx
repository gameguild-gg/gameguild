import React from 'react';

/**
 * L1a: Learning Overview Loading State
 *
 * Streaming fallback while fetching instructor stats and activity.
 * This enables progressive rendering with Suspense boundaries.
 */
export default function Loading(): React.JSX.Element {
  // TODO: Implement skeleton UI for:
  // - KPI cards (4 cards: courses, students, completion rate, rating)
  // - Recent activity list (5-10 skeleton items)
  return <></>;
}
