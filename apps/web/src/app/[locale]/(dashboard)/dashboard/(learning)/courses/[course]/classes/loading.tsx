import React from 'react';

/**
 * Loading UI for Course Classes route.
 * Shown while getCourseClasses() resolves.
 */
export default function Loading(): React.JSX.Element {
  return <div>Loading class schedule...</div>;
}
