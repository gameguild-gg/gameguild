import React from 'react';

/**
 * Course Loading State
 *
 * Streaming fallback while layout awaits getCourse() for navigation context.
 * Child routes have their own loading.tsx for route-specific streaming.
 */
export default function Loading(): React.JSX.Element {
  // TODO: Implement skeleton UI for:
  // - Course header (title, status badge)
  // - Navigation tabs placeholder
  return <></>;
}
