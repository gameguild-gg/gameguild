import React from 'react';

/**
 * L4a: Course Overview Loading State
 *
 * Streaming fallback while fetching course details and analytics.
 * This enables progressive rendering with Suspense boundaries.
 */
export default function Loading(): React.JSX.Element {
  // TODO: Implement skeleton UI for:
  // - Course header (title, status, dates)
  // - Analytics cards (enrollment trend chart, completion funnel, rating distribution, revenue)
  return <></>;
}
