import React from 'react';

/**
 * Loading UI for Course Content route.
 * Shown while getCourseContent() resolves.
 */
export default function Loading(): React.JSX.Element {
  return <div>Loading content structure...</div>;
}
