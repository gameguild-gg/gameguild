import React from 'react';

/**
 * Not Found page for Content Item route.
 * Shown when getContentItem() returns null.
 */
export default function NotFound(): React.JSX.Element {
  return <div>Content item not found.</div>;
}
