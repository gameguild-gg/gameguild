export default async function Default(): Promise<null> {
  // This component is used as a placeholder for the modal in the session layout.
  // It does not render anything and is used to maintain the structure of the layout.
  // The modal will be rendered in the session layout, and this component will not interfere with
  // the rendering of the session details or any other content.
  return null;
}
