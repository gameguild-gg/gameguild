# Behaviour Trees

Behaviour trees are a type of graph algorithm where every node is stateful and can be one of the following types: Selector, Sequence, Parallel, Decorator, Condition, or Action. Every node can be executed (ticked) every frame or at a specific interval.

## Performance Considerations

