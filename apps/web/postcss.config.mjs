/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // Temporarily disabled to fix build issues
    // tailwindcss: {},
    // autoprefixer: {},
  },
};

export default config;
