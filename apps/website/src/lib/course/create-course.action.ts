import { CreateCourseFormState } from '@/lib/course/create-course-form-state';

export async function createCourse(previousState: Readonly<CreateCourseFormState>, formData: FormData): Promise<CreateCourseFormState> {
  return Promise.resolve({});
}
